<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ipro.survey.persistence.dao.project.SurveyExtDao">

    <insert id="insertExt" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.ipro.survey.persistence.model.SurveyExt">
        INSERT INTO tb_survey_ext(
        account,
        ext_key,
        ext_value
        ) VALUES
        (
        #{taskNo},
        #{extKey},
        #{extValue}
        )
    </insert>

</mapper>