<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ipro.survey.persistence.dao.UserProjectRefDao">

    <sql id="columns">
      id,
      project_uniq_no as projectUniqNo,
      user_account as userAccount,
      project_no as projectNo,
      status,
      create_time as createTime,
      update_time as updateTime
    </sql>

    <insert id="insertUserProjectRef" parameterType="com.ipro.survey.persistence.model.User">
        INSERT INTO tb_user_project_ref(
        project_uniq_no,
        user_account,
        project_no,
        status
        ) VALUES
        (
        #{projectUniqNo},
        #{userAccount},
        #{projectNo},
        #{status}
        )
    </insert>

    <select id="selectByProjectUniqNo" resultType="com.ipro.survey.persistence.model.UserProjectRef">
        SELECT
        <include refid="columns"/>
        FROM tb_user_project_ref
        WHERE project_uniq_no = #{projectUniqNo}
    </select>

    <select id="selectByUserAccount" resultType="com.ipro.survey.persistence.model.UserProjectRef">
        SELECT
        <include refid="columns"/>
        FROM tb_user_project_ref
        WHERE user_account = #{userAccount}
    </select>

    <select id="selectByUserAccountAndStatus" resultType="com.ipro.survey.persistence.model.UserProjectRef">
        SELECT
        <include refid="columns"/>
        FROM tb_user_project_ref
        WHERE user_account = #{userAccount} and status=#{status}
    </select>

    <select id="selectValidTaskByAccountAndProjectNo" resultType="com.ipro.survey.persistence.model.UserProjectRef">
        SELECT
        <include refid="columns"/>
        FROM tb_user_project_ref
        WHERE user_account = #{userAccount} AND project_no = #{projectNo} and status = 1
    </select>

    <delete id="deleteByUniqNo">
       DELETE  FROM tb_user_project_ref
       WHERE user_account = "oewo7wMrPRdkfCxLhkQ0qTTMyRME" and status=1
    </delete>

    <update id="invalidByUserAccountAndProjectUniqNo">
        UPDATE tb_user_project_ref
        SET status=0
        WHERE user_account = #{userAccount} and project_uniq_no = #{projectUniqNo}
    </update>
</mapper>