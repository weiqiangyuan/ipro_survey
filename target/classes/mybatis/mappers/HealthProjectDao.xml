<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ipro.survey.persistence.dao.project.HealthProjectDao">

    <sql id="columns">
      id,
      project_no as projectNo,
      project_name as projectName,
      project_desc as projectDesc,
      schedule as schedule,
      schedule_time_level as scheduleTimeLevel,
      create_time as createTime,
      update_time as updateTime
    </sql>

    <select id="selectByProjectNo" resultType="com.ipro.survey.persistence.model.project.HealthProject">
        SELECT
        <include refid="columns"/>
        FROM tb_health_project
        WHERE project_no = #{projectNo}
    </select>

    <insert id="insertProject" parameterType="com.ipro.survey.persistence.model.project.HealthProject">
        INSERT INTO tb_health_project
        (
        project_no,
        project_name,
        project_desc,
        schedule,
        schedule_time_level,
        create_time
        )
        VALUES
        (
        #{projectNo},
        #{projectName},
        #{projectDesc},
        #{schedule},
        #{scheduleTimeLevel},
        #{createTime}
        )
    </insert>

    <update id="updateProject" parameterType="com.ipro.survey.persistence.model.project.HealthProject">
        UPDATE
        tb_health_project
        <set>
            <if test="projectName != null">
                project_name = #{projectName},
            </if>
            <if test="projectDesc != null">
                project_desc = #{projectDesc},
            </if>
            <if test="schedule != null">
                schedule = #{schedule},
            </if>
            <if test="scheduleTimeLevel != null">
                schedule_time_level = #{scheduleTimeLevel},
            </if>
        </set>
        where
        project_no = #{projectNo}
    </update>

    <select id="selectHealthProjectList" resultType="com.ipro.survey.persistence.model.project.HealthProject">
        SELECT
        <include refid="columns"/>
        FROM tb_health_project
        limit 500;
    </select>

</mapper>